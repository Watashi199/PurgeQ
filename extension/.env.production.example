# Build-time URL baked into the extension's default settings.
# Override with the URL of your own self-hosted PurgeQ API, or leave the
# default and let users configure it themselves in Settings.
VITE_API_URL=http://localhost:8000
